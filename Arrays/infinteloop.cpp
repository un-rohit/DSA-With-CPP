#include <iostream>

int main() {
    while (true) {
        std::cout << "This is an infinite loop!" << std::endl;
    }

    // This code will never reach here due to the infinite loop
    return 0;
}
